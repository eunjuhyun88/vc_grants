---
description: Create or refresh the semantic checkpoint before non-trivial implementation work.
---

1. Confirm the active surface and owned path boundary.
2. Run:
   `npm run ctx:checkpoint -- --work-id "<W-ID>" --surface "<surface>" --objective "<objective>"`
3. Include:
   - the canonical docs opened
   - owned files or prefixes
   - decisions made
   - next actions
   - exit criteria
4. If this is a feature branch, confirm there is an active coordination claim.
