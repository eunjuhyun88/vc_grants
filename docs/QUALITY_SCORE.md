# Quality Score

Scale:

- `A`: strong, current, clearly owned
- `B`: mostly strong, some cleanup needed
- `C`: usable but drift-prone
- `D`: active risk
- `F`: missing or misleading

## Current Rubric

| Area | Grade | Notes |
| --- | --- | --- |
| Context routing quality | `C` | Improve as canonical docs become accurate |
| Context handoff quality | `C` | Improve as checkpoints become habitual |
| Context efficiency | `C` | Improve as the small map stays within budget and surface bundles stay narrow |
| Retrieval precision | `C` | Improve as more tasks succeed with the routed small map before code scanning |
| Contextual retrieval quality | `D` | Improve as top retrieval results point to the right canonical docs on ambiguous tasks |
| Agent blueprint clarity | `D` | Improve as outsiders can scaffold and reuse agents without hidden chat context |
| Tool contract coverage | `D` | Improve as reusable tool manifests replace ad hoc wrapper prompts |
| Agent runtime telemetry coverage | `D` | Improve as more measured runs record doc-open counts, runtime, and time saved |
| Infrastructure noise control | `C` | Improve as benchmark runs separate context wins from environment variance |
| Routed-vs-baseline evidence | `D` | Improve as representative tasks are recorded through the A/B report |
| Registry visibility | `C` | Improve as the manifest and query API become accurate and adopted |
| Sandbox policy clarity | `C` | Improve as repo-local execution boundaries are tightened and reviewed |
| Multi-agent coordination safety | `C` | Improve as claims, leases, and path boundaries are used before multi-branch work |
| Doc freshness | `C` | Generated governance should catch drift |
| Harness evidence | `C` | Smoke/browser layer is present; deepen if needed |
